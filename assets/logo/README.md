# Bozos TTS logo

Place the supplied Bozos TTS logo here as:

`assets/logo/bozos-tts.png`

Recommended: the original 320×320 PNG you supplied, with its transparent background preserved.

The renderer automatically uses this file when present and falls back to a text-only mark if it is missing.
